version: 2

sources:
  - name: raw
    description: "Raw tables produced by the upstream PySpark job on Dataproc."
    schema: raw          # the BigQuery dataset
    tables:
      - name: trips_by_hour
        description: "Hourly NYC yellow-taxi aggregates (one row per pickup date + hour)."
